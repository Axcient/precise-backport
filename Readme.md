# Backport packages  and cherry-picked fixes to Precise

Use Docker to help build updated packages against Precise

# Build instructions

## General

```sh
make
```

All packages built in the docker container will appear in the `output` directory.

